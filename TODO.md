# Resume Analyzer — Build Tracker

Tracks the thin end-to-end slice (10 resumes) per `soft-tumbling-brooks` plan.
Update the checkbox and status line as each part completes — this file is the
shared coordination point across parallel work.

## Status legend
`[ ]` not started · `[~]` in progress · `[x]` done · `[!]` blocked

## Tasks

- [x] **Scaffold** — Next.js (TS, App Router, Tailwind) created, git initialized, deps installed (`ai`, `zod`, `pdf-parse`, `pdf-lib`).
- [x] **Shared types** — `src/lib/types.ts` (StatusEntry, ExtractedResume, JobDescription, ScoreBreakdown) — contract for all other tasks.
- [x] **Connectivity spike, superseded** — `scripts/verify-jev-gateway.ts` originally confirmed `typesafe-ai/jev` is listed on the Vercel AI Gateway but blocked on account billing. Since replaced entirely: Jev now goes direct to TypeSafe (`jevClient.ts`, below) and extraction was made fully local (see "Extraction made fully local" below) -- the Gateway is no longer used anywhere in this project. The spike script itself was deleted as obsolete.
- [x] **Dummy resume generator** — first agent attempt fabricated its entire report (claimed files that didn't exist on disk, confirmed via filesystem search). Rebuilt directly in the main session instead: `scripts/generate-dummy-resumes.ts` + `data/job-description.json` (Senior Backend Engineer JD) + 10 PDFs across 3 layouts (sectioned/dense-paragraph/inline-skills) covering strong/borderline/no-match candidates. Verified for real: ran the script (exit 0), listed the 10 files with real sizes, and independently re-extracted text from 4 of them with `pdf-parse` to confirm readable, varied, non-garbled content.
- [x] **Extraction + status store** (agent) — `src/lib/extractor.ts` (`pdf-parse@2.4.5`'s class-based `PDFParse`/`getText()` API + `generateText`/`Output.object` with `openai/gpt-4o-mini` via the Gateway, zod-validated) and `src/lib/statusStore.ts` (`initStatus`/`updateStatus`/`readStatus`, sync fs). Verified: files exist, content matches the report, `tsc --noEmit` clean. **Confirms independently**: the account-wide `customer_verification_required` billing block hits all Gateway completions, not just Jev — needs the credit card added before any real extraction or scoring can run end-to-end. Reconciliation still needed: dashboard's `api/status/route.ts` inlines its own `readStatus()` (written before this file existed) instead of importing this one, and needs a try/catch since this `readStatus()` throws on a missing file rather than returning an empty state.
- [x] **Dashboard UI** (agent) — `src/app/page.tsx`, `src/components/SummaryStats.tsx`, `src/components/ResumeTable.tsx`, `src/components/ResumeDetail.tsx`, `src/app/api/status/route.ts`, `src/app/api/resumes/[id]/route.ts`. Dark (#0a0e0c) + green (#22c55e) theme, status colors (gray/amber/green/red) for pending/in-progress/completed/failed. Verified: files exist, `tsc --noEmit` clean, empty-state page + `/api/status` confirmed live via curl. **Caveat**: `src/lib/statusStore.ts` didn't exist yet when this agent ran, so `api/status/route.ts` and `page.tsx` each inline their own `readStatus()` instead of importing the shared one — needs reconciling once `statusStore.ts` lands (see next task). One inaccuracy in its own report: claimed no dev server was left running, but one was (killed manually, pid 86501/86502).
- [x] **Jev client** (main session) — `src/lib/jevClient.ts`. Better than guessing the wire shape: found that `ai@7.0.107`/`@ai-sdk/gateway@4.0.87` have first-class, typed support for Jev specifically — `GatewayEvaluationModelId` hardcodes `'typesafe-ai/jev'`, and `evaluate()` (exported as `experimental_evaluate`) sends typed Choice/Score/Boolean questions against shared state, matching TypeSafe's own primitives exactly (Boolean = TypeSafe's "Noul"). `askJevScores`/`askJevBooleans` wrap it. `tsc --noEmit` clean. Not yet live-tested (blocked by the same billing gate as the connectivity spike).
- [x] **Jev call logging** (main session, added per user request) — `src/lib/jevLog.ts` records every Jev call's input (state + questions) and output/error to `data/jev-log.json`, keyed by resume id, with `running`/`completed`/`error` status — feeds the dashboard's "what is Jev doing right now" banner and activity panel. Wired into `jevClient.ts`.
- [x] **Scoring** (main session) — `src/lib/scoring.ts` — 3 deterministic rule checks in code (years of experience, required-skill coverage, education level) gate `eligible`; 3 Jev-scored soft dimensions (skill depth 0.45, domain relevance 0.25, seniority fit 0.30) combine into a weighted 0-100 `compositeScore`, per TypeSafe's own composite-scoring pattern. `tsc --noEmit` clean.
- [x] **Pipeline runner** (main session) — `scripts/process-resumes.ts`, extractor -> scoring -> statusStore for all resumes in `data/resumes/`, up to 5 concurrent (per user request) via a small worker-pool helper. Safe under concurrency because `statusStore.ts`'s read-modify-write has no `await` in between, so Node's single-threaded event loop can't interleave two updates.
- [x] **Dashboard/statusStore reconciliation** (main session) — `src/app/api/status/route.ts` and `src/app/page.tsx` now import the real `readStatus()` from `src/lib/statusStore.ts` instead of the temporary inline duplicates.
- [x] **Jev activity UI** (agent) — top banner (`JevNowEvaluating`) showing what Jev is currently evaluating live, plus a right-side `JevActivityPanel` listing every Jev call (running/completed/error) with full input and output shown as scrollable JSON. New `/api/jev-activity` route serves `jevLog.ts`'s data. `page.tsx` now a two-column layout (`lg:grid-cols-[minmax(0,1fr)_minmax(0,26rem)]`). Verified independently: files exist, `tsc`/`lint` clean, fixture + Playwright test artifacts cleaned up. Correctly identified and left alone the pre-existing dev server on :3001 instead of trying to start a duplicate. Flagged (not its own bug) a pre-existing hydration-mismatch console warning in `ResumeTable.tsx`'s timestamp formatting — worth fixing later.
- [x] **Independently-scrollable panels** (agent, per user request) — `lg:h-dvh`/`overflow-hidden` chain from `html` down through the grid (`grid-rows-[minmax(0,1fr)]` is the key fix — an implicit `auto` row won't shrink), each ancestor carrying `min-h-0`; resume table and Jev activity panel scroll internally via their own `overflow-y-auto`, sticky `<thead>` on the table. Mobile (`<lg`) deliberately falls back to normal page scroll instead of stacking two independently-scrolling boxes (bad UX on short viewports) — verified via real scrollTop measurements at 1440x800 and 1440x600 (page never scrolls, panels scroll to distinct positions) and at 390x844 (normal page scroll confirmed). Correctly handled a concurrent edit conflict with the methodology-modal agent by re-reading and layering on top instead of clobbering. Verified independently: `tsc`/`lint` clean, fixture data properly restored (`status.json` back to 10 real entries, `jev-log.json`/Playwright artifacts deleted).
- [x] **Scoring methodology, single source of truth** (main session, per user request) — refactored `src/lib/scoring.ts` so `RULE_CHECK_DEFINITIONS` and `DIMENSION_DEFINITIONS` are the one place that defines both the real rule checks/Jev questions AND the data behind a new `getScoringMethodology()` export, so "what we tell the user we check" can never drift from "what we actually check." New `src/app/api/methodology/route.ts` serves it. Verified: `tsc` clean, live-curled on the running dev server with real data returned (works without the Gateway/billing fix since it doesn't call any model).
- [x] **"What is Jev evaluating" modal** (agent) — "What is Jev evaluating?" trigger at the top of `JevActivityPanel`, opens `AnalysisMethodologyModal` (fetches `/api/methodology` once on mount, closes via button/backdrop/Escape, all independently verified with Playwright including both close paths tested separately). Verified: files exist, wired in correctly, `tsc`/`lint` clean (a transient tsc/lint error this agent reported was a real race against my concurrent edit to `process-resumes.ts` at the time — confirmed clean on a fresh re-run, not an actual bug).
- [x] **Worker pool tracking** (main session, per user request) — `src/lib/workerStatus.ts` (`data/worker-status.json`, same sync-fs pattern as statusStore/jevLog) + `src/app/api/workers/route.ts`. `scripts/process-resumes.ts`'s pool now gives each concurrent slot a stable `workerId` for its whole run (not a fresh id per item), reporting `idle`/`extracting`/`scoring` + which resume it's on. Verified: `tsc` clean, ran the real pipeline (concurrency 5, all failed at the known billing wall) and confirmed all 5 slots correctly settled back to `idle`, live-curled `/api/workers` on the running dev server.
- [x] **Right-panel restructure** (agent, per user request) — right column is now: methodology header -> `WorkerPoolPanel` (new, polls `/api/workers`, count driven by `workers.length` not hardcoded, idle/extracting/scoring color+pulse) -> "Job Activity Log" (reframed `JevActivityPanel` entries, `LifecycleLine` shows "Request sent, waiting for response..." / "Response received · completed" / "Response received · failed" — honest to the real running/completed/error states, no fabricated intermediate stages). Only the log scrolls internally. Verified independently: files exist, `tsc`/`lint` clean, real measured scroll values confirm page never scrolls (innerHeight == scrollHeight) while the log's container scrolls independently, worker-status/jev-log fixtures properly cleaned up/restored.
- [x] **Jev scoring, unblocked** (main session, per user request) — rewrote `src/lib/jevClient.ts` to call TypeSafe's direct API (`api.typesafe.ai/v1/systemone`, `TYPESAFE_API_KEY`) instead of the Vercel Gateway's `evaluate()`, bypassing the Gateway billing block entirely for the scoring step. Wire shapes (`type: "noul"` not `"boolean"`; `score` response field is raw `[0, levels.length-1]`, not pre-normalized) taken verbatim from `docs.typesafe.ai`, not guessed. **Live-tested for real**: a standalone call returned an actual Jev score (2.99/4, confidence 0.97, with full `legend`/`probabilities`) — first real (non-billing-blocked) model response in this project. `tsc`/`lint` clean. Test log entry cleaned from `data/jev-log.json` afterward.
- [x] **Pipeline runnable from the UI, with per-stage timing** (main session, per user request) — extracted the pipeline's logic out of `scripts/process-resumes.ts` into `src/lib/pipeline.ts` (`runPipeline()`, `isPipelineRunning()`, in-memory run guard against overlapping runs), so it can run in-process inside the Next.js server, not just as a standalone CLI script. New `src/app/api/run/route.ts`: `GET` reports whether a run is in flight, `POST` starts one (fire-and-forget). Also added `StageTimings` (`extractionMs`/`scoringMs`/`totalMs`) to `StatusEntry` and `ResumeResult`, captured per resume.
- [x] **"Run Pipeline" button + timing display** (agent, per user request) — `RunPipelineButton.tsx` polls `/api/run`, optimistic UI on click, pulsing "Running..." state matching existing conventions; timings shown as a Time column in `ResumeTable` and a breakdown in `ResumeDetail`. Verified independently: files exist, `tsc`/`lint` clean; agent's own Playwright run captured a real click -> `running:true` -> `running:false` cycle with matching `status.json` timestamps, and fixture/backup restore left `status.json` byte-identical to the original (confirmed via diff) -- the one loose end (an unexplained extra run mid-test) was investigated and traced to normal testing activity, not a code bug (button component reviewed line-by-line: no auto-POST path exists, only the explicit click handler calls POST).
- [x] **pdf-parse/Turbopack bundling bug, fixed** (main session) — the Run button surfaced a real bug: triggering the pipeline through the Next.js server (not the standalone CLI script) failed with `Cannot find module '.../pdf.worker.mjs'`, because Turbopack tried to bundle `pdf-parse`'s pdfjs-dist worker file and broke its runtime resolution. Fixed per `pdf-parse`'s own Next.js/serverless guidance: added `serverExternalPackages: ["pdf-parse"]` to `next.config.ts`. Required a dev-server restart -- done without disrupting the in-flight UI agent. Verified: re-triggered `/api/run`, confirmed the error field now correctly shows the actual (separate) Gateway billing error instead of the module-resolution crash.
- [x] **Persistent Jev activity log** (main session, per user request) — `jevLog.ts` previously kept only the *latest* call per resume (`id: "resumeId:score"`, overwritten every run) -- re-running the pipeline silently erased prior runs' history. Fixed: `logJevCallStart` now generates a `crypto.randomUUID()` per call; nothing is ever overwritten. Verified: two calls for the same resume both persisted as distinct entries.
- [x] **Extraction made fully local, Vercel Gateway removed entirely** (main session, per user request) — rewrote `src/lib/extractor.ts` from an LLM-based extractor (via the Gateway) to a pure local heuristic parser: `pdf-parse` for raw text (unchanged), then plain regex/section-header parsing for structured fields -- zero external API call for extraction. **Known, accepted tradeoff**: the two "dense prose" dummy resumes (no section headers, deliberately built to stress-test extraction) can't be reliably parsed by heuristics -- confirmed in the real run below (they scored anomalously low). Also: `workerStatus.ts` tracking removed from the extraction stage in `pipeline.ts` -- the worker pool now only reflects the Jev-calling stage, per explicit request ("workers are for only jev calls to track"). Cleanup: deleted the now-obsolete `scripts/verify-jev-gateway.ts`, uninstalled `ai`/`@ai-sdk/gateway`/`zod` (confirmed unused via grep first), removed `AI_GATEWAY_API_KEY` from `.env.local`/`.env.local.example`/`docs/SETUP.md`. **First real full end-to-end run**: all 10 resumes reached `completed` with real Jev scores (97, 81, 55, 54→26, 40, 4, 1, 1, 0, 0) -- ranking is sensible except the two dense-layout resumes scoring near 0 despite being strong/borderline candidates, exactly the predicted tradeoff. Verified live through the actual dashboard server (`POST /api/run`), not just the CLI.
- [x] **SummaryStats not updating live, fixed** (main session) — the top summary-counts strip was a plain server-rendered component with no polling at all (unlike every other dashboard component), so it stayed frozen at whatever the counts were on initial page load while the table below it updated live -- this is what the user was seeing as "still showing 10 failed." Converted to a client component polling `/api/status` every 1500ms, same pattern as `ResumeTable`. Verified: server-rendered HTML now shows live counts matching `data/status.json` exactly (Total 10 / Completed 10 / Failed 0), hot-reloaded with no server restart needed.
- [x] **End-to-end run** — no longer blocked. Extraction is local (no external dependency), Jev scoring goes direct to TypeSafe. `npx tsx scripts/process-resumes.ts` or the dashboard's "Run Pipeline" button both work for real now.

- [x] **Run-archive + replay simulator** (main session, per user request: "keep every log so we can create a dummy run pipeline simulator") — every pipeline run now gets a `runId` (shared run-guard moved to `src/lib/runContext.ts`, used by both real runs and replays so they can never overlap). `src/lib/runArchive.ts` permanently snapshots each run's final status entries, its Jev calls, and every completed resume's full result to `data/runs/<runId>.json` (never overwritten, unlike the live `status.json`). `src/lib/simulator.ts` replays an archived run back into the live status/worker/jev-log files with **zero real API calls** -- paced by the original run's actual per-stage timings (scaled by a `speed` factor) so it animates with the same relative rhythm, not an instant flash. Every replayed Jev-log entry is tagged `simulated: true` so it's never confused with a real call. New routes: `GET /api/runs` (list archives), `POST /api/simulate` (replay one). **Verified fully for real**: ran the real pipeline (archived it), listed it via `/api/runs`, triggered a replay via `/api/simulate`, confirmed `status.json` reached `completed` with the exact original composite scores (82/1/0/3/40/0/97/1/27/53) and `jev-log.json` gained exactly 10 new entries all correctly flagged `simulated: true`, and confirmed the run-guard (`/api/run` -> `running: false`) settled back to idle afterward.

- [x] **`eligible` surfaced in status data** (main session, per user request) — added `eligible?: boolean` to `StatusEntry` (previously only inside the full `ScoreBreakdown`, not available to the lightweight table view), populated in both `pipeline.ts` and `simulator.ts`. `tsc`/`lint` clean. Feeds the sort/filter toolbar below.
- [x] **"Best fit" sort + score/eligibility filter** (agent, per user request) — toolbar above `ResumeTable`: "Best fit" sort (4 buckets: eligible-completed by score desc, then ineligible-completed by score desc, then in-progress, then failed -- eligibility deliberately trumps raw score, not hidden by it, since the separate "eligible only" toggle already covers hiding), "Filename" sort (insertion-order reset), minimum-score filter (completed rows only; in-progress/failed always pass through), "eligible only" toggle, live "Showing X of Y" count. All client-side (cheap at 1000 rows), derived via `useMemo` from the existing poll -- no changes to polling itself. Verified independently: `tsc`/`lint` clean (an earlier transient syntax error was this agent mid-edit, confirmed resolved), a 35-entry fixture with deliberately independent score/eligible combinations proved the bucket ordering for real (a score-100-but-ineligible resume correctly ranked *below* a score-5-but-eligible one in the rendered DOM), min-score and eligibility filters both produced exactly the expected counts, `status.json` restored byte-identical to the original afterward.
- [x] **1000-resume generation** (agent, per user request) — `scripts/generate-dummy-resumes.ts` rewritten as a programmatic/seeded generator (mulberry32 PRNG, seed 20260921 for reproducibility): name/company/skill/education pools across 4 fit tiers (strong/good/borderline/weak) and the existing 3 layouts, exact target distribution via precomputed counts rather than per-item random draws. `data/resumes-manifest.json` records ground truth (fitTier/layout/yearsExperience per resume) as a side channel never written into the PDFs themselves. Verified independently: `tsc`/`lint` clean *for this file*, real run took ~1.3s, exactly 1000 PDFs (`resume-0001.pdf`..`resume-1000.pdf`), old `resume-01`..`resume-10.pdf` confirmed gone, manifest has exactly 1000 unique entries with fitTier counts `{good:180, strong:120, borderline:300, weak:400}` and layout counts `{sectioned:500, dense:170, inline:330}` matching the intended ~12/18/30/40% and ~17% dense targets, `du -sh` shows a reasonable 3.9M. Spot-checked 5 real extracted-text samples across tiers -- coherent, tier-appropriate content, zero ground-truth leakage into PDF text. Ready for a real 1000-resume pipeline run.

## Docs

- [x] `TODO.md` (this file)
- [~] `docs/SETUP.md` — env var setup (no secret values), install/run instructions
